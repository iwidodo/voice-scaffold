# CSV Database Architecture

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    APPLICATION LAYER                             │
│  (Streamlit UI, API Endpoints, Services)                        │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       │ Query APIs
                       │
┌──────────────────────▼──────────────────────────────────────────┐
│                 DATABASE INTERFACE                               │
│  - get_all_providers()        - get_provider_schedule()         │
│  - get_provider_by_id()       - get_available_slots()           │
│  - get_providers_by_specialty() - book_slot()                   │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       │ In-Memory Access
                       │
┌──────────────────────▼──────────────────────────────────────────┐
│              IN-MEMORY DATABASE                                  │
│                                                                  │
│  PROVIDERS_DB: List[Provider]                                   │
│  ├─ Provider(id="p001", name="Dr. Sarah Johnson", ...)         │
│  ├─ Provider(id="p002", name="Dr. Michael Chen", ...)          │
│  └─ ...                                                         │
│                                                                  │
│  SCHEDULES_DB: Dict[str, List[Schedule]]                       │
│  ├─ "p001" → [Schedule(date="2026-01-06", ...), ...]           │
│  ├─ "p002" → [Schedule(date="2026-01-06", ...), ...]           │
│  └─ ...                                                         │
└──────────────────────▲──────────────────────────────────────────┘
                       │
                       │ Load on Startup / Reload
                       │
┌──────────────────────┴──────────────────────────────────────────┐
│                   CSV FILES (Disk)                               │
│                                                                  │
│  ┌─────────────────────┐    ┌────────────────────────┐         │
│  │  providers.csv      │    │  schedules.csv         │         │
│  ├─────────────────────┤    ├────────────────────────┤         │
│  │ id,name,specialty...│    │ provider_id,date,...   │         │
│  │ p001,Dr. Sarah...   │    │ p001,2026-01-06,...    │         │
│  │ p002,Dr. Michael... │    │ p001,2026-01-07,...    │         │
│  │ ...                 │    │ ...                    │         │
│  └─────────────────────┘    └────────────────────────┘         │
│                                                                  │
│  Optional: save_schedules_to_csv() for persistence              │
└──────────────────────────────────────────────────────────────────┘
```

## Data Flow

### Startup Flow
```
1. Application starts
   │
   ├─> import backend.database.providers
   │   └─> initialize_database()
   │       └─> load_providers_from_csv()
   │           └─> PROVIDERS_DB populated
   │
   └─> import backend.database.schedules
       └─> initialize_database()
           └─> load_schedules_from_csv()
               └─> SCHEDULES_DB populated
```

### Query Flow
```
User Request
   │
   └─> get_provider_by_id("p001")
       │
       └─> Search PROVIDERS_DB (in-memory)
           │
           └─> Return Provider object (< 1ms)
```

### Booking Flow
```
User Books Appointment
   │
   └─> book_slot("p001", "2026-01-06", "09:00")
       │
       ├─> Find schedule in SCHEDULES_DB
       ├─> Remove time slot from available_slots
       └─> [Optional] save_schedules_to_csv()
```

### Reload Flow
```
Need Fresh Data
   │
   └─> clear_schedule_cache()
       │
       └─> initialize_database()
           │
           └─> load_schedules_from_csv()
               │
               └─> SCHEDULES_DB refreshed from disk
```

## Component Diagram

```
┌──────────────────────────────────────────────────────────┐
│                  Backend Package                          │
│                                                           │
│  ┌─────────────────────────────────────────────────────┐ │
│  │              database/                               │ │
│  │                                                      │ │
│  │  ┌─────────────────┐    ┌─────────────────┐       │ │
│  │  │  providers.py   │    │  schedules.py   │       │ │
│  │  │                 │    │                 │       │ │
│  │  │ • load_csv()    │    │ • load_csv()    │       │ │
│  │  │ • get_all()     │    │ • get_schedule()│       │ │
│  │  │ • get_by_id()   │    │ • get_slots()   │       │ │
│  │  │ • get_by_spec() │    │ • book_slot()   │       │ │
│  │  └─────────────────┘    └─────────────────┘       │ │
│  │           │                       │                │ │
│  │           ├───────────────────────┤                │ │
│  │           ▼                       ▼                │ │
│  │  ┌──────────────────────────────────────┐         │ │
│  │  │         __init__.py                  │         │ │
│  │  │  • Exports all public functions      │         │ │
│  │  │  • Clean API for other modules       │         │ │
│  │  └──────────────────────────────────────┘         │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                           │
│  ┌─────────────────────────────────────────────────────┐ │
│  │              llm/                                    │ │
│  │  • provider_matcher.py (uses database)              │ │
│  │  • conversation_manager.py                          │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                           │
│  ┌─────────────────────────────────────────────────────┐ │
│  │              services/                               │ │
│  │  • appointment_service.py (uses database)           │ │
│  │  • schedule_service.py (uses database)              │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                           │
│  ┌─────────────────────────────────────────────────────┐ │
│  │              api/                                    │ │
│  │  • appointments.py (uses services)                  │ │
│  │  • conversation.py (uses services)                  │ │
│  └─────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

## Performance Characteristics

```
┌────────────────────┬──────────────┬─────────────┐
│ Operation          │ Time         │ Complexity  │
├────────────────────┼──────────────┼─────────────┤
│ Load CSV (startup) │ ~10ms        │ O(n)        │
│ Get all providers  │ ~0.1ms       │ O(1)        │
│ Get provider by ID │ ~0.1ms       │ O(n)        │
│ Get by specialty   │ ~0.2ms       │ O(n)        │
│ Get schedule       │ ~0.1ms       │ O(1)        │
│ Get available slots│ ~0.1ms       │ O(n)        │
│ Book slot          │ ~0.2ms       │ O(n)        │
│ Save to CSV        │ ~5ms         │ O(n)        │
│ Reload from CSV    │ ~10ms        │ O(n)        │
└────────────────────┴──────────────┴─────────────┘

Note: Times are approximate for dataset size of 10 providers
      and 50 schedule entries. O(n) is negligible at this scale.
```

## Memory Footprint

```
┌─────────────────────┬──────────────────────┐
│ Component           │ Memory Usage         │
├─────────────────────┼──────────────────────┤
│ Provider Objects    │ ~2 KB (10 providers) │
│ Schedule Objects    │ ~8 KB (50 entries)   │
│ Python Overhead     │ ~5 KB                │
│ Total In-Memory     │ ~15 KB               │
└─────────────────────┴──────────────────────┘

CSV Files on Disk:
├─ providers.csv      ~1 KB
└─ schedules.csv      ~4 KB
```

## Comparison with Alternatives

```
┌──────────────┬─────────────┬──────────────┬─────────────┐
│ Approach     │ Setup       │ Query Speed  │ Demo Value  │
├──────────────┼─────────────┼──────────────┼─────────────┤
│ CSV (Current)│ ★★★★★       │ ★★★★★        │ ★★★★★       │
│ SQLite       │ ★★★★☆       │ ★★★★★        │ ★★★★☆       │
│ PostgreSQL   │ ★★☆☆☆       │ ★★★★★        │ ★★★☆☆       │
│ Mock/Gen     │ ★★★★★       │ ★★★★★        │ ★★☆☆☆       │
└──────────────┴─────────────┴──────────────┴─────────────┘
```

## Benefits Visualization

```
                CSV-Based Database

    ┌─────────────────────────────────────────┐
    │                                          │
    │  📝 Easy to Edit                         │
    │  • Open in Excel/Text Editor             │
    │  • No special tools needed               │
    │  • Visual inspection                     │
    │                                          │
    ├─────────────────────────────────────────┤
    │                                          │
    │  ⚡ Fast Queries                         │
    │  • In-memory lookups                     │
    │  • < 1ms response time                   │
    │  • No database overhead                  │
    │                                          │
    ├─────────────────────────────────────────┤
    │                                          │
    │  🎯 Demo Perfect                         │
    │  • Clear data visibility                 │
    │  • Easy to explain                       │
    │  • Professional presentation             │
    │                                          │
    ├─────────────────────────────────────────┤
    │                                          │
    │  🔄 Version Control                      │
    │  • Git-friendly format                   │
    │  • Track changes easily                  │
    │  • Diff-able                             │
    │                                          │
    ├─────────────────────────────────────────┤
    │                                          │
    │  🚀 Migration Ready                      │
    │  • Same API for DB upgrade               │
    │  • Clear path to production              │
    │  • No code rewrites needed               │
    │                                          │
    └─────────────────────────────────────────┘
```

## Usage Example Flow

```
1. User: "I have a rash"
          │
          ▼
   [Provider Matcher]
          │
          ├─> Search ISSUE_TO_SPECIALTY
          │   → "rash" → Dermatologist
          │
          ├─> get_providers_by_specialty("Dermatologist")
          │   → PROVIDERS_DB lookup
          │   → [Dr. Sarah Johnson, Dr. Michael Chen]
          │
          └─> Select best (highest rating)
              → Dr. Sarah Johnson (4.8/5.0)
              
2. User: "Show me available times"
          │
          ▼
   [Schedule Service]
          │
          ├─> get_provider_schedule("p001")
          │   → SCHEDULES_DB lookup
          │   → [5 dates]
          │
          └─> Present dates to user
          
3. User: "Book January 6 at 9:00 AM"
          │
          ▼
   [Appointment Service]
          │
          ├─> book_slot("p001", "2026-01-06", "09:00")
          │   → Find in SCHEDULES_DB
          │   → Remove from available_slots
          │   → Return success
          │
          └─> Create appointment confirmation
              → Email, Calendar invite, etc.
```

This architecture provides the perfect balance of:
- **Simplicity** for demos
- **Performance** for queries
- **Flexibility** for modifications
- **Scalability** for future growth
